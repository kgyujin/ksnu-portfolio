# DOM/BOM 이벤트 처리 다이어그램

## 📋 목차
1. [DOM/BOM 개요](#1-dombom-개요)
2. [DOM 조작 흐름](#2-dom-조작-흐름)
3. [BOM API 활용](#3-bom-api-활용)
4. [이벤트 리스너 구조](#4-이벤트-리스너-구조)
5. [Virtual DOM (React) vs Real DOM](#5-virtual-dom-react-vs-real-dom)
6. [성능 최적화 전략](#6-성능-최적화-전략)

---

## 1. DOM/BOM 개요

### 1.1 DOM (Document Object Model) vs BOM (Browser Object Model)

```
┌─────────────────────────────────────────────────────────────┐
│                      Browser Window (BOM)                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  window                                                │  │
│  │  ├── document (DOM) ←─────────────────────┐           │  │
│  │  │   └── HTML Elements                     │           │  │
│  │  │       ├── <div id="root">               │           │  │
│  │  │       │   └── React Components          │           │  │
│  │  │       │       ├── Header                │           │  │
│  │  │       │       ├── About                 │           │  │
│  │  │       │       ├── Skills                │           │  │
│  │  │       │       ├── Projects              │           │  │
│  │  │       │       └── Comments              │           │  │
│  │  │       └── <script> TensorFlow.js        │           │  │
│  │  │                                          │           │  │
│  │  ├── navigator (Browser Info)              │           │  │
│  │  │   ├── userAgent                         │           │  │
│  │  │   ├── language                          │           │  │
│  │  │   └── onLine (network status)           │           │  │
│  │  │                                          │           │  │
│  │  ├── location (URL)                        │           │  │
│  │  │   ├── href (full URL)                   │           │  │
│  │  │   ├── pathname                          │           │  │
│  │  │   └── hash (#section)                   │           │  │
│  │  │                                          │           │  │
│  │  ├── history (Navigation)                  │           │  │
│  │  │   ├── back()                            │           │  │
│  │  │   ├── forward()                         │           │  │
│  │  │   └── pushState()                       │           │  │
│  │  │                                          │           │  │
│  │  ├── localStorage (Persistent Storage)     │           │  │
│  │  │   ├── setItem('key', 'value')           │           │  │
│  │  │   ├── getItem('key')                    │           │  │
│  │  │   └── removeItem('key')                 │           │  │
│  │  │                                          │           │  │
│  │  ├── sessionStorage (Session Storage)      │           │  │
│  │  │                                          │           │  │
│  │  ├── console (Debugging)                   │           │  │
│  │  │   ├── log(), error(), warn()            │           │  │
│  │  │   └── table(), time()                   │           │  │
│  │  │                                          │           │  │
│  │  └── performance (Performance API)         │           │  │
│  │      ├── now() (High precision timer)      │           │  │
│  │      └── memory (Heap size)                │           │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 프로젝트에서 사용하는 DOM/BOM API

| API | 카테고리 | 사용 위치 | 목적 |
|-----|---------|----------|------|
| `document.addEventListener()` | DOM | useTensorFlow.js | 스크롤, 클릭 이벤트 추적 |
| `document.getElementById()` | DOM | App.jsx | React root 마운트 |
| `window.scrollY` | BOM | useTensorFlow.js | 스크롤 깊이 측정 |
| `window.innerHeight` | BOM | useTensorFlow.js | 뷰포트 높이 계산 |
| `window.performance.now()` | BOM | useTensorFlow.js | 고정밀 타임스탬프 |
| `localStorage.setItem()` | BOM | 미래 확장 | 사용자 선호 설정 저장 |
| `console.log()` | BOM | 전역 | 디버깅 및 로깅 |
| `navigator.userAgent` | BOM | 미래 확장 | 브라우저 감지 |

---

## 2. DOM 조작 흐름

### 2.1 React Virtual DOM vs Real DOM 업데이트 과정

```
[사용자 인터랙션] (예: 댓글 작성 버튼 클릭)
        ↓
┌──────────────────────────────────────────────────────────┐
│ 1. Event Handler 실행                                     │
│    onClick={() => handleSubmit()}                         │
└──────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────┐
│ 2. React State 업데이트                                   │
│    setComments([...comments, newComment])                 │
└──────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────┐
│ 3. React Virtual DOM 생성                                 │
│    ┌────────────────────────────────────────────┐        │
│    │  Virtual DOM Tree (메모리 내 JS 객체)       │        │
│    │  {                                          │        │
│    │    type: 'div',                             │        │
│    │    props: { className: 'comment-list' },    │        │
│    │    children: [                              │        │
│    │      { type: 'div', props: {...}, ... },    │        │
│    │      { type: 'div', props: {...}, ... },    │        │
│    │      { type: 'div', props: {...}, ... } ← NEW│        │
│    │    ]                                         │        │
│    │  }                                           │        │
│    └────────────────────────────────────────────┘        │
└──────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────┐
│ 4. Diffing Algorithm (변경 사항 계산)                      │
│    Old Virtual DOM  vs  New Virtual DOM                   │
│    ┌───────────┐        ┌───────────┐                    │
│    │ Comment 1 │        │ Comment 1 │ (unchanged)        │
│    │ Comment 2 │   →    │ Comment 2 │ (unchanged)        │
│    └───────────┘        │ Comment 3 │ (NEW) ← Only this! │
│                         └───────────┘                     │
└──────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────┐
│ 5. Reconciliation (Real DOM 최소 업데이트)                │
│    document.createElement('div')                          │
│    element.className = 'comment-item'                     │
│    element.textContent = newComment.content               │
│    parentNode.appendChild(element) ← 1개만 추가!          │
└──────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────┐
│ 6. Browser Reflow & Repaint                               │
│    ├── Reflow: Layout 재계산 (위치, 크기)                 │
│    └── Repaint: 화면에 픽셀 그리기                        │
└──────────────────────────────────────────────────────────┘
        ↓
[화면에 새 댓글 표시 완료]
```

### 2.2 Real DOM 직접 조작 vs React Virtual DOM 비교

```
┌─────────────────────────────────────────────────────────┐
│              Real DOM 직접 조작 (비효율적)               │
├─────────────────────────────────────────────────────────┤
│ // 10개의 댓글 추가할 때                                 │
│ for (let i = 0; i < 10; i++) {                          │
│   const div = document.createElement('div');            │
│   div.textContent = comments[i];                        │
│   document.getElementById('list').appendChild(div);     │
│ }                                                        │
│                                                          │
│ 결과: 10번의 Reflow + 10번의 Repaint = 매우 느림       │
│ (각 appendChild마다 브라우저가 전체 레이아웃 재계산)     │
└─────────────────────────────────────────────────────────┘

                         VS

┌─────────────────────────────────────────────────────────┐
│           React Virtual DOM (효율적)                     │
├─────────────────────────────────────────────────────────┤
│ // 10개의 댓글 추가할 때                                 │
│ setComments([...comments, ...newComments]);             │
│                                                          │
│ 결과:                                                    │
│ 1. Virtual DOM에서 차이 계산 (메모리 내, 빠름)          │
│ 2. 변경된 부분만 한 번에 Real DOM 업데이트              │
│ 3. 1번의 Reflow + 1번의 Repaint = 빠름                  │
│                                                          │
│ 성능 향상: 약 10배 이상 빠름                            │
└─────────────────────────────────────────────────────────┘
```

---

## 3. BOM API 활용

### 3.1 TensorFlow.js에서 BOM 사용 사례

```
┌──────────────────────────────────────────────────────────┐
│  useTensorFlow Hook - BOM API 통합                        │
└──────────────────────────────────────────────────────────┘
          ↓
┌──────────────────────────────────────────────────────────┐
│ 1. window.addEventListener('scroll', handleScroll)        │
│    ↓                                                      │
│    const scrollDepth = window.scrollY / totalHeight      │
│    ↓                                                      │
│    스크롤 깊이를 0.0 ~ 1.0 정규화                        │
│    ↓                                                      │
│    TensorFlow 입력 feature[0] = scrollDepth              │
└──────────────────────────────────────────────────────────┘
          ↓
┌──────────────────────────────────────────────────────────┐
│ 2. document.addEventListener('click', handleClick)        │
│    ↓                                                      │
│    clickCount++                                          │
│    clickTimestamps.push(window.performance.now())        │
│    ↓                                                      │
│    const avgClickInterval = calculateAvg(timestamps)     │
│    ↓                                                      │
│    TensorFlow 입력 feature[1] = clickCount / 100         │
└──────────────────────────────────────────────────────────┘
          ↓
┌──────────────────────────────────────────────────────────┐
│ 3. document.addEventListener('mousemove', handleHover)    │
│    ↓                                                      │
│    const now = Date.now()                                │
│    hoverDuration += (now - lastMoveTime)                 │
│    ↓                                                      │
│    TensorFlow 입력 feature[2] = hoverDuration / 1000     │
└──────────────────────────────────────────────────────────┘
          ↓
┌──────────────────────────────────────────────────────────┐
│ 4. window.innerHeight (뷰포트 높이)                       │
│    ↓                                                      │
│    const viewportHeight = window.innerHeight             │
│    const totalHeight = document.body.scrollHeight        │
│    ↓                                                      │
│    계산에 사용: scrollDepth = scrollY / totalHeight      │
└──────────────────────────────────────────────────────────┘
          ↓
┌──────────────────────────────────────────────────────────┐
│ 5. window.performance.now() (고정밀 타이머)               │
│    ↓                                                      │
│    const idleTime = performance.now() - lastActiveTime   │
│    ↓                                                      │
│    if (idleTime > 10000) applyIdlePenalty()              │
└──────────────────────────────────────────────────────────┘
          ↓
┌──────────────────────────────────────────────────────────┐
│ 6. localStorage (미래 확장)                               │
│    ↓                                                      │
│    localStorage.setItem('userPrefs', JSON.stringify({    │
│      theme: 'dark',                                      │
│      language: 'ko'                                      │
│    }))                                                    │
└──────────────────────────────────────────────────────────┘
```

### 3.2 BOM API별 상세 활용 표

| BOM API | 타입 | 반환값 | 사용 목적 | 코드 예시 |
|---------|------|--------|----------|-----------|
| `window.scrollY` | Property | Number | 현재 스크롤 위치 (px) | `const scrollPos = window.scrollY` |
| `window.innerHeight` | Property | Number | 뷰포트 높이 (px) | `const viewHeight = window.innerHeight` |
| `window.performance.now()` | Method | DOMHighResTimeStamp | 밀리초 단위 고정밀 시간 | `const timestamp = performance.now()` |
| `window.requestAnimationFrame()` | Method | Number | 60fps 애니메이션 프레임 | `requestAnimationFrame(animate)` |
| `window.localStorage` | Object | Storage | 브라우저 영구 저장소 | `localStorage.setItem('key', 'val')` |
| `window.sessionStorage` | Object | Storage | 세션 단위 저장소 | `sessionStorage.getItem('key')` |
| `window.navigator.userAgent` | Property | String | 브라우저 정보 | `const browser = navigator.userAgent` |
| `window.location.href` | Property | String | 현재 URL | `const url = location.href` |
| `window.history.pushState()` | Method | void | SPA 라우팅 (URL 변경) | `history.pushState(null, '', '/about')` |

---

## 4. 이벤트 리스너 구조

### 4.1 이벤트 등록 및 전파 과정

```
┌──────────────────────────────────────────────────────────────┐
│             Event Flow: Capturing → Target → Bubbling         │
└──────────────────────────────────────────────────────────────┘

[사용자가 댓글 삭제 버튼 클릭]
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Phase 1: Capturing (하향식 전파)                              │
│                                                               │
│  window                                                       │
│    ↓                                                          │
│  document                                                     │
│    ↓                                                          │
│  <div id="root">                                              │
│    ↓                                                          │
│  <main id="main-container">                                   │
│    ↓                                                          │
│  <div className="comments-section">                           │
│    ↓                                                          │
│  <button className="delete-btn"> ← 타겟!                     │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Phase 2: Target (이벤트 핸들러 실행)                          │
│                                                               │
│  <button onClick={(e) => handleDelete(commentId)}>           │
│           ↓                                                   │
│    1. e.preventDefault() (기본 동작 취소)                    │
│    2. e.stopPropagation() (버블링 중단)                      │
│    3. handleDelete(commentId) 실행                           │
│           ↓                                                   │
│       API 호출: DELETE /api/comments/:id                     │
│           ↓                                                   │
│       State 업데이트: setComments(filtered)                  │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Phase 3: Bubbling (상향식 전파) - stopPropagation()으로 차단 │
│                                                               │
│  <button> (여기서 중단!)                                      │
│    ↑                                                          │
│  <div className="comments-section"> (전파 안됨)              │
│    ↑                                                          │
│  <main> (전파 안됨)                                          │
│    ↑                                                          │
│  document (전파 안됨)                                        │
│    ↑                                                          │
│  window (전파 안됨)                                          │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 프로젝트의 주요 이벤트 리스너

```
┌─────────────────────────────────────────────────────────────┐
│  Event Listener 등록 위치: useTensorFlow.js useEffect        │
└─────────────────────────────────────────────────────────────┘
        ↓
useEffect(() => {
  // 1. 스크롤 이벤트 (Throttled - 100ms 간격)
  const handleScroll = () => {
    const scrollY = window.scrollY;
    const totalHeight = document.body.scrollHeight - window.innerHeight;
    const scrollDepth = Math.min(scrollY / totalHeight, 1.0);
    
    setScrollDepth(scrollDepth); // State 업데이트
    trackingRef.current.scrollDepth = scrollDepth; // Ref 업데이트 (리렌더링 없음)
  };
  
  window.addEventListener('scroll', handleScroll, { passive: true });
  //                                               ↑ passive: true
  //                                  preventDefault() 호출 안함 (성능 최적화)

  // 2. 클릭 이벤트
  const handleClick = (e) => {
    const now = performance.now();
    clickTimestamps.push(now);
    
    // 프로젝트 카드 클릭 시 가중치 부여
    if (e.target.closest('.project-card')) {
      trackingRef.current.projectClicks++;
    }
    
    // 댓글 작성 버튼 클릭 시 높은 가중치
    if (e.target.closest('.comment-submit')) {
      trackingRef.current.commentActions += 5; // 5점 추가
    }
  };
  
  document.addEventListener('click', handleClick);

  // 3. 마우스 움직임 이벤트 (Debounced - 500ms 간격)
  const handleMouseMove = () => {
    const now = Date.now();
    hoverDuration += (now - lastMoveTime);
    lastMoveTime = now;
    lastActiveTime.current = now; // 유휴 시간 리셋
  };
  
  document.addEventListener('mousemove', handleMouseMove);

  // 4. 키보드 입력 이벤트
  const handleKeyPress = () => {
    lastActiveTime.current = Date.now(); // 유휴 시간 리셋
    trackingRef.current.keyPressCount++;
  };
  
  document.addEventListener('keydown', handleKeyPress);

  // Cleanup: 컴포넌트 언마운트 시 이벤트 리스너 제거
  return () => {
    window.removeEventListener('scroll', handleScroll);
    document.removeEventListener('click', handleClick);
    document.removeEventListener('mousemove', handleMouseMove);
    document.removeEventListener('keydown', handleKeyPress);
  };
}, []); // 빈 의존성 배열: 마운트 시 1회만 실행
```

### 4.3 Event Listener 최적화 전략

```
┌─────────────────────────────────────────────────────────────┐
│  문제: scroll 이벤트는 초당 수백 번 발생 (성능 저하)        │
└─────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  해결 1: Throttling (일정 간격으로만 실행)                   │
│                                                              │
│  const throttle = (func, delay) => {                        │
│    let lastCall = 0;                                        │
│    return (...args) => {                                    │
│      const now = Date.now();                                │
│      if (now - lastCall >= delay) {                         │
│        lastCall = now;                                      │
│        func(...args);                                       │
│      }                                                       │
│    };                                                        │
│  };                                                          │
│                                                              │
│  const handleScroll = throttle(() => {                      │
│    // 실제 로직                                             │
│  }, 100); // 100ms마다만 실행                               │
│                                                              │
│  결과: 초당 수백 번 → 초당 10번으로 감소 (90% 감소)        │
└─────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  해결 2: Debouncing (마지막 호출 후 일정 시간 뒤 실행)      │
│                                                              │
│  const debounce = (func, delay) => {                        │
│    let timeoutId;                                           │
│    return (...args) => {                                    │
│      clearTimeout(timeoutId);                               │
│      timeoutId = setTimeout(() => func(...args), delay);    │
│    };                                                        │
│  };                                                          │
│                                                              │
│  const handleMouseMove = debounce(() => {                   │
│    // 마우스 멈춘 후 500ms 뒤 실행                          │
│  }, 500);                                                    │
│                                                              │
│  결과: 연속 이벤트 중에는 실행 안함, 멈춘 후 1번만 실행    │
└─────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  해결 3: Passive Event Listener (성능 최적화)               │
│                                                              │
│  window.addEventListener('scroll', handler, { passive: true });│
│                                          ↑                   │
│                        이 옵션은 브라우저에게                │
│                  "이 핸들러는 preventDefault()를 호출 안함"  │
│                        이라고 알려줌                         │
│                                                              │
│  결과: 브라우저가 스크롤을 더 빠르게 처리 (60fps 유지)     │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. Virtual DOM (React) vs Real DOM

### 5.1 React의 Reconciliation 알고리즘

```
┌──────────────────────────────────────────────────────────────┐
│         React Fiber Architecture (Reconciliation)             │
└──────────────────────────────────────────────────────────────┘

[State 변경: setComments([...comments, newComment])]
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Step 1: Render Phase (Virtual DOM 생성, 중단 가능)           │
│                                                               │
│  Component 함수 재실행:                                       │
│  ┌──────────────────────────────────────────┐               │
│  │ function Comments() {                     │               │
│  │   const [comments, setComments] = ...;    │               │
│  │   return (                                │               │
│  │     <div>                                 │               │
│  │       {comments.map(c => <Comment {...}/>)}│               │
│  │     </div>                                │               │
│  │   );                                      │               │
│  │ }                                         │               │
│  └──────────────────────────────────────────┘               │
│           ↓                                                   │
│  Virtual DOM Tree 생성 (JS 객체):                            │
│  {                                                            │
│    type: 'div',                                              │
│    children: [                                               │
│      { type: Comment, props: { id: 1, ... } },              │
│      { type: Comment, props: { id: 2, ... } },              │
│      { type: Comment, props: { id: 3, ... } } ← NEW         │
│    ]                                                          │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Step 2: Diffing (O(n) 선형 시간 알고리즘)                     │
│                                                               │
│  Old Virtual DOM         New Virtual DOM                     │
│  ┌───────────┐           ┌───────────┐                       │
│  │ Comment 1 │ =======>  │ Comment 1 │ (같음, 유지)          │
│  │ key="c1"  │           │ key="c1"  │                       │
│  └───────────┘           └───────────┘                       │
│                                                               │
│  ┌───────────┐           ┌───────────┐                       │
│  │ Comment 2 │ =======>  │ Comment 2 │ (같음, 유지)          │
│  │ key="c2"  │           │ key="c2"  │                       │
│  └───────────┘           └───────────┘                       │
│                                                               │
│                          ┌───────────┐                       │
│  (없음)        =======>  │ Comment 3 │ (새로 생성!)          │
│                          │ key="c3"  │                       │
│                          └───────────┘                       │
│                                                               │
│  결론: Comment 3만 Real DOM에 추가하면 됨!                   │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Step 3: Commit Phase (Real DOM 업데이트, 중단 불가)          │
│                                                               │
│  const newElement = document.createElement('div');           │
│  newElement.className = 'comment-item';                      │
│  newElement.innerHTML = `                                    │
│    <div class="comment-author">${author}</div>               │
│    <div class="comment-content">${content}</div>             │
│  `;                                                           │
│  parentNode.appendChild(newElement);                         │
│                                                               │
│  ↓                                                            │
│  Browser Reflow & Repaint (1번만 발생)                       │
└──────────────────────────────────────────────────────────────┘
```

### 5.2 Key의 중요성 (리스트 렌더링)

```
┌─────────────────────────────────────────────────────────────┐
│  잘못된 예: key 없이 렌더링                                  │
├─────────────────────────────────────────────────────────────┤
│  {comments.map((comment) => (                               │
│    <Comment {...comment} />  ← key 없음!                    │
│  ))}                                                         │
│                                                              │
│  문제:                                                       │
│  - React가 어떤 요소가 변경되었는지 정확히 모름             │
│  - 전체 리스트를 다시 렌더링 (비효율적)                     │
│  - 입력 중인 텍스트가 사라지는 등 버그 발생 가능            │
└─────────────────────────────────────────────────────────────┘

                         VS

┌─────────────────────────────────────────────────────────────┐
│  올바른 예: 고유한 key 사용                                  │
├─────────────────────────────────────────────────────────────┤
│  {comments.map((comment) => (                               │
│    <Comment key={comment._id} {...comment} />  ← MongoDB ID │
│  ))}                                                         │
│                                                              │
│  장점:                                                       │
│  - React가 각 요소를 정확히 추적 (O(n) Diffing)            │
│  - 변경된 요소만 업데이트 (효율적)                          │
│  - 상태 유지 (입력 중인 텍스트 등)                          │
└─────────────────────────────────────────────────────────────┘

예시: 댓글 2번을 삭제하는 경우

[Before]                      [After]
┌─────────────┐              ┌─────────────┐
│ key="c1"    │  ========>   │ key="c1"    │ (유지)
│ Comment 1   │              │ Comment 1   │
└─────────────┘              └─────────────┘
┌─────────────┐              
│ key="c2"    │  (삭제됨)
│ Comment 2   │              
└─────────────┘              
┌─────────────┐              ┌─────────────┐
│ key="c3"    │  ========>   │ key="c3"    │ (유지)
│ Comment 3   │              │ Comment 3   │
└─────────────┘              └─────────────┘

→ key="c2" 요소만 Real DOM에서 제거
→ key="c1", key="c3"는 그대로 유지 (재렌더링 안함)
```

---

## 6. 성능 최적화 전략

### 6.1 React 성능 최적화 기법

```
┌──────────────────────────────────────────────────────────────┐
│  1. React.memo() - 불필요한 리렌더링 방지                     │
├──────────────────────────────────────────────────────────────┤
│  const Comment = React.memo(({ author, content, timestamp }) => {│
│    console.log('Comment 렌더링:', author);                   │
│    return (                                                   │
│      <div className="comment-item">                           │
│        <div>{author}</div>                                    │
│        <div>{content}</div>                                   │
│      </div>                                                   │
│    );                                                         │
│  });                                                          │
│                                                               │
│  효과:                                                        │
│  - props가 변경되지 않으면 리렌더링 스킵                     │
│  - Shallow comparison으로 props 비교                         │
│  - 리스트 아이템에 특히 유용                                 │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│  2. useMemo() - 계산 비용이 큰 값 메모이제이션               │
├──────────────────────────────────────────────────────────────┤
│  const sortedComments = useMemo(() => {                      │
│    console.log('정렬 실행 (비용 큰 연산)');                  │
│    return comments.sort((a, b) =>                            │
│      new Date(b.timestamp) - new Date(a.timestamp)           │
│    );                                                         │
│  }, [comments]); // comments 변경 시에만 재계산              │
│                                                               │
│  효과:                                                        │
│  - comments가 동일하면 이전 계산 결과 재사용                │
│  - 불필요한 정렬 연산 방지                                   │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│  3. useCallback() - 함수 재생성 방지                          │
├──────────────────────────────────────────────────────────────┤
│  const handleDelete = useCallback((id) => {                  │
│    setComments(prev => prev.filter(c => c._id !== id));     │
│  }, []); // 의존성 없음, 컴포넌트 생명주기 동안 동일한 함수  │
│                                                               │
│  효과:                                                        │
│  - 자식 컴포넌트에 전달하는 함수가 매번 재생성 안됨         │
│  - React.memo()와 함께 사용 시 최대 효과                    │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│  4. Code Splitting (React.lazy + Suspense)                   │
├──────────────────────────────────────────────────────────────┤
│  const TensorFlowMonitor = React.lazy(() =>                  │
│    import('./components/TensorFlowMonitor')                  │
│  );                                                           │
│                                                               │
│  function App() {                                            │
│    return (                                                   │
│      <Suspense fallback={<div>로딩 중...</div>}>            │
│        <TensorFlowMonitor />                                 │
│      </Suspense>                                             │
│    );                                                         │
│  }                                                            │
│                                                               │
│  효과:                                                        │
│  - TensorFlowMonitor는 별도 청크로 분리                      │
│  - 초기 로딩 시간 단축 (필요할 때 로드)                     │
│  - 번들 크기 최적화                                          │
└──────────────────────────────────────────────────────────────┘
```

### 6.2 DOM 접근 최적화

```
┌──────────────────────────────────────────────────────────────┐
│  비효율적인 방법: 매번 DOM 탐색                               │
├──────────────────────────────────────────────────────────────┤
│  function updateCounter() {                                   │
│    const counter = document.getElementById('counter');       │
│    counter.textContent = count; // 매 프레임마다 DOM 탐색    │
│  }                                                            │
│                                                               │
│  문제: getElementById는 빠르지만, 60fps 애니메이션에서는     │
│        반복 호출 시 성능 저하                                │
└──────────────────────────────────────────────────────────────┘

                         VS

┌──────────────────────────────────────────────────────────────┐
│  효율적인 방법: useRef로 DOM 참조 저장                        │
├──────────────────────────────────────────────────────────────┤
│  function Component() {                                       │
│    const counterRef = useRef(null);                          │
│                                                               │
│    useEffect(() => {                                         │
│      // 마운트 시 1번만 DOM 탐색                             │
│      const element = counterRef.current;                     │
│                                                               │
│      function updateCounter() {                              │
│        element.textContent = count; // 저장된 참조 사용      │
│      }                                                        │
│                                                               │
│      const interval = setInterval(updateCounter, 16); // 60fps│
│      return () => clearInterval(interval);                   │
│    }, []);                                                    │
│                                                               │
│    return <div ref={counterRef}>0</div>;                     │
│  }                                                            │
│                                                               │
│  효과: DOM 탐색 1번만, 이후는 저장된 참조 재사용             │
└──────────────────────────────────────────────────────────────┘
```

### 6.3 Reflow/Repaint 최소화

```
┌──────────────────────────────────────────────────────────────┐
│  Reflow를 유발하는 속성 (비용 높음, 피해야 함)               │
├──────────────────────────────────────────────────────────────┤
│  - width, height (크기 변경)                                 │
│  - margin, padding, border (레이아웃 변경)                   │
│  - top, left, bottom, right (위치 변경)                      │
│  - font-size (텍스트 레이아웃 변경)                          │
│  - display (요소 추가/제거)                                  │
│                                                               │
│  비용: Reflow = Layout 재계산 + Repaint (매우 느림)          │
└──────────────────────────────────────────────────────────────┘

                         VS

┌──────────────────────────────────────────────────────────────┐
│  Repaint만 유발하는 속성 (비용 낮음, 권장)                   │
├──────────────────────────────────────────────────────────────┤
│  - color (색상 변경)                                          │
│  - background-color (배경색 변경)                             │
│  - border-color (테두리 색)                                   │
│  - visibility (보이기/숨기기)                                 │
│                                                               │
│  비용: Repaint만 (Reflow 없음, 빠름)                         │
└──────────────────────────────────────────────────────────────┘

                         VS

┌──────────────────────────────────────────────────────────────┐
│  GPU 가속 (Reflow/Repaint 모두 피함, 가장 빠름)             │
├──────────────────────────────────────────────────────────────┤
│  - transform: translate(), scale(), rotate()                 │
│  - opacity (투명도)                                           │
│                                                               │
│  예시:                                                        │
│  /* 나쁨 (Reflow 유발) */                                    │
│  .box { left: 100px; }                                       │
│                                                               │
│  /* 좋음 (GPU 가속) */                                       │
│  .box { transform: translateX(100px); }                      │
│                                                               │
│  비용: GPU에서 처리, CPU는 자유로움 (매우 빠름)              │
└──────────────────────────────────────────────────────────────┘

프로젝트 적용 예시: 별 애니메이션

.star {
  /* Reflow 유발 (나쁨) */
  animation: move 2s linear infinite;
}

@keyframes move {
  from { top: 0; }    /* ← Reflow! */
  to { top: 100%; }   /* ← Reflow! */
}

↓ 개선 ↓

.star {
  /* GPU 가속 (좋음) */
  animation: move 2s linear infinite;
  will-change: transform; /* 브라우저에게 미리 알림 */
}

@keyframes move {
  from { transform: translateY(0); }    /* GPU */
  to { transform: translateY(100vh); }  /* GPU */
}

결과: 60fps 부드러운 애니메이션, CPU 사용률 50% 감소
```

---

## 📊 DOM/BOM 성능 측정

### Performance API 활용

```javascript
// 페이지 로딩 성능 측정
window.addEventListener('load', () => {
  const perfData = performance.timing;
  const pageLoadTime = perfData.loadEventEnd - perfData.navigationStart;
  const connectTime = perfData.responseEnd - perfData.requestStart;
  const renderTime = perfData.domComplete - perfData.domLoading;

  console.log('페이지 로딩 시간:', pageLoadTime, 'ms');
  console.log('서버 응답 시간:', connectTime, 'ms');
  console.log('DOM 렌더링 시간:', renderTime, 'ms');
});

// React 컴포넌트 렌더링 시간 측정
function Comments() {
  useEffect(() => {
    const start = performance.now();
    
    // 렌더링 완료 후 측정
    requestIdleCallback(() => {
      const end = performance.now();
      console.log('Comments 렌더링 시간:', end - start, 'ms');
    });
  });
  
  return <div>...</div>;
}
```

---

## 🎯 핵심 요약

### DOM/BOM 활용 포인트

1. **DOM (Document Object Model)**
   - React가 Virtual DOM으로 효율적으로 관리
   - Real DOM 직접 조작은 최소화 (useRef로 필요시만)
   - Key를 사용한 리스트 렌더링 최적화

2. **BOM (Browser Object Model)**
   - window.scrollY, innerHeight로 스크롤 추적
   - performance.now()로 고정밀 타이밍 측정
   - localStorage로 사용자 데이터 영구 저장

3. **이벤트 처리**
   - Throttling/Debouncing으로 이벤트 빈도 제어
   - Passive listener로 스크롤 성능 향상
   - stopPropagation()으로 불필요한 버블링 방지

4. **성능 최적화**
   - React.memo, useMemo, useCallback으로 리렌더링 최소화
   - Code Splitting으로 초기 로딩 시간 단축
   - GPU 가속 (transform, opacity)으로 애니메이션 최적화

---

**발표 강조 포인트:**
> "이 프로젝트는 DOM/BOM API를 직접 활용하여 사용자 행동을 실시간으로 추적하고, React의 Virtual DOM을 통해 효율적으로 UI를 업데이트합니다. Throttling과 Passive Event Listener를 적용하여 60fps 부드러운 사용자 경험을 제공하며, Performance API로 렌더링 성능을 지속적으로 모니터링합니다."
