# Git 브랜치 전략 & 협업 워크플로우

## 📋 목차
1. [Git 브랜치 전략 개요](#1-git-브랜치-전략-개요)
2. [브랜치 모델 (Git Flow)](#2-브랜치-모델-git-flow)
3. [Commit Convention](#3-commit-convention)
4. [Pull Request 프로세스](#4-pull-request-프로세스)
5. [CI/CD와 Git 통합](#5-cicd와-git-통합)
6. [Git 명령어 워크플로우](#6-git-명령어-워크플로우)

---

## 1. Git 브랜치 전략 개요

### 1.1 프로젝트의 Git 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    GitHub Repository                         │
│              github.com/kgyujin/ksnu-portfolio              │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                  Branch Structure                            │
│                                                              │
│  main (프로덕션)                                             │
│    ├── develop (개발)                                        │
│    │     ├── feature/tensorflow-integration                │
│    │     ├── feature/comment-system                        │
│    │     ├── feature/kubernetes-deployment                 │
│    │     └── feature/mongodb-integration                   │
│    │                                                         │
│    ├── hotfix/security-patch (긴급 수정)                    │
│    └── release/v1.0.0 (릴리스 준비)                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                 GitHub Actions Triggers                      │
│                                                              │
│  Push to main       → 프로덕션 배포 (Vercel + K8s)         │
│  Push to develop    → 개발 서버 배포 (Staging)              │
│  Pull Request       → 자동 테스트 + 코드 리뷰               │
│  Tag (v*.*.*)       → 릴리스 노트 생성                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 브랜치별 역할 및 보호 규칙

| 브랜치 | 목적 | 배포 대상 | 보호 규칙 | 수명 |
|--------|------|----------|----------|------|
| `main` | 프로덕션 릴리스 | Vercel + K8s (프로덕션) | PR 필수, 리뷰 2명 이상, CI 통과 필수 | 영구 |
| `develop` | 개발 통합 브랜치 | 스테이징 서버 | PR 필수, CI 통과 필수 | 영구 |
| `feature/*` | 새 기능 개발 | 로컬 개발 환경 | 없음 | 기능 완성 후 삭제 |
| `hotfix/*` | 긴급 버그 수정 | 프로덕션 (즉시) | PR 필수, 긴급 승인 가능 | 수정 완료 후 삭제 |
| `release/*` | 릴리스 준비 | 스테이징 → 프로덕션 | PR 필수, QA 완료 필수 | 릴리스 후 삭제 |

---

## 2. 브랜치 모델 (Git Flow)

### 2.1 전체 워크플로우 다이어그램

```
시간 흐름 (위→아래)

main ────●──────────────────────────●──────────────────●────
         │                          ↑                  ↑
       v1.0.0                   merge              v1.1.0
         │                      (PR#42)                │
         │                          │                  │
         ↓                          │                  │
develop ─●────●────●────●────●─────●────●────●───────●────
         │    ↑    ↑    ↑    ↑      │    ↑    ↑        │
         │    │    │    │    │      │    │    │        │
         │   merge merge │   merge  │   merge │       merge
         │   (PR)  (PR)  │   (PR)   │   (PR)  │       (PR)
         │    │    │     │    │     │    │    │        │
         │    │    │     │    │     │    │    │        │
feature/ │    │    │     │    │     │    │    │        │
tensorflow    │    │     │    │     │    │    │        │
    ─────●────●────●     │    │     │    │    │        │
    (9 commits)          │    │     │    │    │        │
                         │    │     │    │    │        │
feature/                 │    │     │    │    │        │
comment-system           │    │     │    │    │        │
    ─────────────────────●────●     │    │    │        │
         (15 commits)                │    │    │        │
                                     │    │    │        │
feature/                             │    │    │        │
kubernetes                           │    │    │        │
    ─────────────────────────────────●────●    │        │
              (7 commits)                      │        │
                                               │        │
feature/                                       │        │
mongodb-integration                            │        │
    ───────────────────────────────────────────●────────●
                    (12 commits)

Legend:
● = Commit
→ = Branch creation
↑ = Merge (Pull Request)
```

### 2.2 각 단계별 상세 흐름

#### Phase 1: Feature 개발

```
[개발자의 로컬 환경]

1. develop 브랜치에서 새 feature 브랜치 생성
   ┌─────────────────────────────────────────┐
   │ $ git checkout develop                  │
   │ $ git pull origin develop               │
   │ $ git checkout -b feature/tensorflow    │
   └─────────────────────────────────────────┘
            ↓
2. 기능 개발 (여러 커밋)
   ┌─────────────────────────────────────────┐
   │ $ git add src/hooks/useTensorFlow.js    │
   │ $ git commit -m "feat: Add TensorFlow   │
   │   model initialization"                 │
   │                                          │
   │ $ git add src/components/Monitor.jsx    │
   │ $ git commit -m "feat: Add real-time    │
   │   monitoring UI"                        │
   │                                          │
   │ $ git add tests/tensorflow.test.js      │
   │ $ git commit -m "test: Add unit tests   │
   │   for TensorFlow model"                 │
   └─────────────────────────────────────────┘
            ↓
3. 로컬에서 테스트 실행
   ┌─────────────────────────────────────────┐
   │ $ npm test                              │
   │ ✓ TensorFlow model initializes          │
   │ ✓ Behavior tracking works               │
   │ ✓ Interest score calculates correctly   │
   │                                          │
   │ $ npm run lint                          │
   │ ✓ No linting errors                     │
   └─────────────────────────────────────────┘
            ↓
4. 원격 저장소에 push
   ┌─────────────────────────────────────────┐
   │ $ git push origin feature/tensorflow    │
   └─────────────────────────────────────────┘
```

#### Phase 2: Pull Request 생성

```
[GitHub 웹 인터페이스]

1. Pull Request 생성
   ┌─────────────────────────────────────────────────────────┐
   │  Title: feat: Implement TensorFlow.js interest tracking │
   │                                                          │
   │  Description:                                            │
   │  ## 변경 사항                                            │
   │  - TensorFlow.js 모델 초기화 (9→32→16→8→1)             │
   │  - 9가지 행동 패턴 추적 (스크롤, 클릭, 호버 등)        │
   │  - 실시간 관심도 점수 계산 (0-100%)                     │
   │  - 유휴 시간 감지 및 패널티 적용                       │
   │                                                          │
   │  ## 테스트                                               │
   │  - ✓ 단위 테스트 통과 (coverage 95%)                    │
   │  - ✓ 로컬 환경에서 동작 확인                            │
   │                                                          │
   │  ## 스크린샷                                             │
   │  [TensorFlowMonitor UI 스크린샷]                        │
   │                                                          │
   │  Closes #23 (TensorFlow.js 통합 이슈)                   │
   └─────────────────────────────────────────────────────────┘
            ↓
2. 자동 CI 파이프라인 실행
   ┌─────────────────────────────────────────────────────────┐
   │  GitHub Actions                                          │
   │  ┌────────────────────────────────────────────────┐     │
   │  │ ✓ Install dependencies (npm ci)                │     │
   │  │ ✓ Run linter (ESLint)                          │     │
   │  │ ✓ Run unit tests (Jest)                        │     │
   │  │ ✓ Build frontend (Vite)                        │     │
   │  │ ✓ Build backend (Docker)                       │     │
   │  │ ✓ Security scan (Trivy)                        │     │
   │  │                                                 │     │
   │  │ All checks passed ✓                            │     │
   │  └────────────────────────────────────────────────┘     │
   └─────────────────────────────────────────────────────────┘
            ↓
3. 코드 리뷰 요청
   ┌─────────────────────────────────────────────────────────┐
   │  Reviewers:                                              │
   │  - @reviewer1 (requested)                               │
   │  - @reviewer2 (requested)                               │
   │                                                          │
   │  Labels: [enhancement] [tensorflow] [frontend]          │
   └─────────────────────────────────────────────────────────┘
```

#### Phase 3: 코드 리뷰

```
[코드 리뷰 과정]

1. 리뷰어 1의 피드백
   ┌─────────────────────────────────────────────────────────┐
   │  📝 Comment on src/hooks/useTensorFlow.js:75            │
   │                                                          │
   │  "이 부분에서 memory leak 가능성이 있습니다.            │
   │   useEffect cleanup에서 텐서를 명시적으로 dispose해야   │
   │   합니다."                                               │
   │                                                          │
   │  Suggestion:                                             │
   │  ```js                                                   │
   │  return () => {                                          │
   │    model.dispose();  // ← 추가                          │
   │    tf.disposeVariables();                               │
   │  };                                                      │
   │  ```                                                     │
   └─────────────────────────────────────────────────────────┘
            ↓
2. 개발자의 수정
   ┌─────────────────────────────────────────────────────────┐
   │  $ git add src/hooks/useTensorFlow.js                   │
   │  $ git commit -m "fix: Add tensor disposal in cleanup"  │
   │  $ git push origin feature/tensorflow                   │
   │                                                          │
   │  → PR에 자동으로 새 커밋 추가됨                         │
   │  → CI 파이프라인 다시 실행                              │
   └─────────────────────────────────────────────────────────┘
            ↓
3. 리뷰어 승인
   ┌─────────────────────────────────────────────────────────┐
   │  @reviewer1 approved ✓                                  │
   │  "LGTM! (Looks Good To Me) 메모리 관리가 잘 되어        │
   │   있습니다."                                             │
   │                                                          │
   │  @reviewer2 approved ✓                                  │
   │  "테스트 커버리지도 충분하고, 코드 품질 우수합니다."    │
   └─────────────────────────────────────────────────────────┘
```

#### Phase 4: Merge 및 배포

```
[Merge 프로세스]

1. Squash and Merge (develop로)
   ┌─────────────────────────────────────────────────────────┐
   │  Merge method: Squash and merge                         │
   │                                                          │
   │  Before (9 commits):                                     │
   │  - feat: Add TensorFlow model initialization            │
   │  - feat: Add behavior tracking                          │
   │  - feat: Add interest score calculation                 │
   │  - feat: Add monitoring UI                              │
   │  - test: Add unit tests                                 │
   │  - docs: Update README                                  │
   │  - fix: Memory leak in useEffect                        │
   │  - refactor: Extract constants                          │
   │  - style: Format code                                   │
   │                                                          │
   │  After (1 commit):                                       │
   │  - feat: Implement TensorFlow.js interest tracking (#42)│
   │                                                          │
   │  → develop 브랜치에 1개의 깔끔한 커밋으로 병합          │
   └─────────────────────────────────────────────────────────┘
            ↓
2. 자동 배포 (develop → Staging)
   ┌─────────────────────────────────────────────────────────┐
   │  GitHub Actions: Deploy to Staging                      │
   │  ┌────────────────────────────────────────────────┐     │
   │  │ ✓ Build Docker image                           │     │
   │  │ ✓ Push to Docker Hub (tag: develop-abc123)     │     │
   │  │ ✓ Deploy to Staging K8s cluster                │     │
   │  │ ✓ Run smoke tests                              │     │
   │  │                                                 │     │
   │  │ Deployment successful ✓                        │     │
   │  │ Staging URL: https://staging.ksnu-portfolio.com│     │
   │  └────────────────────────────────────────────────┘     │
   └─────────────────────────────────────────────────────────┘
            ↓
3. Release 준비 (develop → release/v1.1.0)
   ┌─────────────────────────────────────────────────────────┐
   │  $ git checkout develop                                 │
   │  $ git pull origin develop                              │
   │  $ git checkout -b release/v1.1.0                       │
   │  $ git push origin release/v1.1.0                       │
   │                                                          │
   │  → QA 팀이 릴리스 브랜치에서 최종 테스트                │
   └─────────────────────────────────────────────────────────┘
            ↓
4. 프로덕션 배포 (release → main)
   ┌─────────────────────────────────────────────────────────┐
   │  Pull Request: release/v1.1.0 → main                    │
   │                                                          │
   │  Title: Release v1.1.0                                  │
   │                                                          │
   │  Release Notes:                                          │
   │  ## New Features                                         │
   │  - TensorFlow.js interest tracking (#42)                │
   │  - Real-time monitoring UI (#43)                        │
   │                                                          │
   │  ## Bug Fixes                                            │
   │  - Fix comment deletion issue (#44)                     │
   │                                                          │
   │  ## Performance                                          │
   │  - Improve initial load time by 30%                     │
   │                                                          │
   │  Merge → main                                            │
   └─────────────────────────────────────────────────────────┘
            ↓
5. Git Tag 및 프로덕션 배포
   ┌─────────────────────────────────────────────────────────┐
   │  $ git tag -a v1.1.0 -m "Release v1.1.0"                │
   │  $ git push origin v1.1.0                               │
   │                                                          │
   │  GitHub Actions: Deploy to Production                   │
   │  ┌────────────────────────────────────────────────┐     │
   │  │ ✓ Build Docker image (tag: v1.1.0)             │     │
   │  │ ✓ Push to Docker Hub                           │     │
   │  │ ✓ Deploy to Production K8s cluster             │     │
   │  │ ✓ Update Vercel deployment                     │     │
   │  │ ✓ Run health checks                            │     │
   │  │ ✓ Send Slack notification                      │     │
   │  │                                                 │     │
   │  │ Production deployment successful ✓             │     │
   │  │ Live URL: https://ksnu-portfolio.com           │     │
   │  └────────────────────────────────────────────────┘     │
   └─────────────────────────────────────────────────────────┘
```

---

## 3. Commit Convention

### 3.1 Conventional Commits 규칙

```
┌─────────────────────────────────────────────────────────────┐
│              Commit Message 구조                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  <type>(<scope>): <subject>                                 │
│  <BLANK LINE>                                                │
│  <body>                                                      │
│  <BLANK LINE>                                                │
│  <footer>                                                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘

예시:

feat(tensorflow): Add interest score prediction model

- Implement 9-feature neural network (9→32→16→8→1)
- Track user behavior (scroll, click, hover, idle)
- Calculate weighted interest score (70% behavior + 30% NN)
- Add memory management (tensor disposal)

Closes #23
```

### 3.2 Commit Type 분류

| Type | 설명 | 예시 | Semantic Version |
|------|------|------|------------------|
| `feat` | 새로운 기능 추가 | `feat(api): Add comment delete endpoint` | MINOR (1.1.0) |
| `fix` | 버그 수정 | `fix(ui): Fix comment sorting order` | PATCH (1.0.1) |
| `docs` | 문서 변경 | `docs(readme): Update installation guide` | - |
| `style` | 코드 포맷팅 (기능 변경 없음) | `style(css): Format comments.css` | - |
| `refactor` | 코드 리팩토링 (기능 변경 없음) | `refactor(hooks): Extract useFetch hook` | - |
| `test` | 테스트 추가/수정 | `test(api): Add comment controller tests` | - |
| `chore` | 빌드 설정, 의존성 업데이트 | `chore(deps): Update react to 18.3.0` | - |
| `perf` | 성능 개선 | `perf(tensorflow): Optimize model inference` | PATCH (1.0.1) |
| `ci` | CI/CD 설정 변경 | `ci(actions): Add Docker build caching` | - |
| `build` | 빌드 시스템 변경 | `build(vite): Enable code splitting` | - |
| `revert` | 이전 커밋 되돌리기 | `revert: Revert "feat: Add feature X"` | - |

### 3.3 실제 Commit 예시

```bash
# 1. TensorFlow.js 기능 추가
git commit -m "feat(tensorflow): Implement behavior tracking system

- Add event listeners for scroll, click, hover events
- Implement idle detection (10s threshold)
- Calculate normalized feature vector (9 dimensions)
- Integrate with TensorFlow.js model for prediction

Refs #23"

# 2. 댓글 삭제 버그 수정
git commit -m "fix(comments): Fix delete button not working

The delete button was not triggering the API call due to
incorrect event handler binding. Changed from arrow function
to useCallback to maintain stable reference.

Fixes #44"

# 3. Dockerfile 멀티스테이지 빌드 최적화
git commit -m "perf(docker): Reduce image size by 85%

- Use multi-stage build (deps → builder → production)
- Remove dev dependencies from final image
- Use alpine base image instead of ubuntu
- Image size: 1.2GB → 150MB

Performance improvement: 7x faster deployment"

# 4. CI/CD 파이프라인에 보안 스캔 추가
git commit -m "ci(security): Add Trivy vulnerability scanning

- Scan Docker images for CVE vulnerabilities
- Fail pipeline if critical vulnerabilities found
- Generate security report artifact

Closes #67"

# 5. 테스트 커버리지 개선
git commit -m "test(api): Increase test coverage to 95%

- Add integration tests for comment CRUD operations
- Add edge case tests (empty input, long strings)
- Mock MongoDB with mongodb-memory-server

Coverage: 72% → 95%"
```

---

## 4. Pull Request 프로세스

### 4.1 PR 템플릿

```markdown
## 변경 사항 요약
[변경 사항을 간단히 설명]

## 변경 타입
- [ ] 새 기능 (feat)
- [ ] 버그 수정 (fix)
- [ ] 성능 개선 (perf)
- [ ] 리팩토링 (refactor)
- [ ] 문서 업데이트 (docs)
- [ ] 테스트 추가/수정 (test)
- [ ] CI/CD 변경 (ci)

## 체크리스트
- [ ] 로컬에서 테스트 완료
- [ ] ESLint 통과
- [ ] 테스트 커버리지 80% 이상
- [ ] README/문서 업데이트 (필요 시)
- [ ] Breaking change 없음 (또는 CHANGELOG 업데이트)

## 테스트 방법
[리뷰어가 기능을 테스트할 수 있는 방법 설명]

## 스크린샷 (UI 변경 시)
[Before/After 스크린샷 첨부]

## 관련 이슈
Closes #이슈번호
Refs #관련이슈
```

### 4.2 PR 리뷰 프로세스

```
┌──────────────────────────────────────────────────────────────┐
│ PR 생성 (feature/tensorflow → develop)                        │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ 자동 체크 (GitHub Actions)                                    │
│  ├── ✓ ESLint (코드 품질)                                     │
│  ├── ✓ Jest (단위 테스트)                                     │
│  ├── ✓ Cypress (E2E 테스트)                                   │
│  ├── ✓ Docker Build (이미지 빌드 가능한지)                    │
│  └── ✓ Trivy (보안 스캔)                                      │
│                                                               │
│  All checks passed ✓                                          │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ 코드 리뷰 (2명 이상 승인 필요)                                │
│                                                               │
│  Reviewer 1:                                                  │
│  ├── 📝 Comment: "memory leak 우려"                           │
│  └── 🔄 Request changes                                       │
│                                                               │
│  [개발자 수정 후 재push]                                      │
│                                                               │
│  Reviewer 1:                                                  │
│  └── ✅ Approved                                              │
│                                                               │
│  Reviewer 2:                                                  │
│  └── ✅ Approved                                              │
│                                                               │
│  Status: 2/2 approvals ✓                                      │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Merge 옵션 선택                                               │
│                                                               │
│  ○ Merge commit (모든 커밋 히스토리 유지, develop에 사용)   │
│  ● Squash and merge (1개 커밋으로 압축, feature에 사용) ←   │
│  ○ Rebase and merge (선형 히스토리, main에 사용)            │
│                                                               │
│  [Squash and merge 클릭]                                     │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ 자동 배포 (develop 브랜치에 merge 후)                         │
│                                                               │
│  GitHub Actions: deploy-staging.yml                          │
│  ├── Build Docker image (develop-sha256)                     │
│  ├── Push to Docker Hub                                      │
│  ├── Deploy to Kubernetes (staging namespace)                │
│  ├── Health check (HTTP 200 확인)                            │
│  └── Slack notification: "Staging 배포 완료 ✓"              │
└──────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│ Feature 브랜치 삭제                                           │
│                                                               │
│  GitHub: "Delete branch" 버튼 클릭                           │
│  또는                                                         │
│  $ git branch -d feature/tensorflow (로컬)                   │
│  $ git push origin --delete feature/tensorflow (원격)        │
└──────────────────────────────────────────────────────────────┘
```

---

## 5. CI/CD와 Git 통합

### 5.1 GitHub Actions 트리거 구조

```yaml
# .github/workflows/ci-cd.yml

name: CI/CD Pipeline

on:
  # 1. PR 생성 시 (모든 브랜치 → develop 또는 main)
  pull_request:
    branches: [develop, main]
    types: [opened, synchronize, reopened]
  
  # 2. Push 시 (develop 또는 main에 직접 push)
  push:
    branches: [develop, main]
  
  # 3. Tag 생성 시 (v로 시작하는 태그)
  push:
    tags:
      - 'v*.*.*'
  
  # 4. 수동 트리거 (GitHub UI에서 실행 버튼)
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - staging
          - production

jobs:
  # Job 1: 코드 품질 검사 (모든 트리거에서 실행)
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run lint
      - run: npm test -- --coverage
      - run: npm run build
  
  # Job 2: 보안 스캔 (모든 트리거에서 실행)
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm audit --production
      - run: docker build -t temp-image .
      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'temp-image'
          severity: 'CRITICAL,HIGH'
  
  # Job 3: Staging 배포 (develop 브랜치에 push 시)
  deploy-staging:
    needs: [test, security]
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v3
      - run: docker build -t ksnu-portfolio:develop-${{ github.sha }} .
      - run: docker push ksnu-portfolio:develop-${{ github.sha }}
      - run: kubectl set image deployment/api api=ksnu-portfolio:develop-${{ github.sha }} -n staging
  
  # Job 4: Production 배포 (main 브랜치 또는 v태그에 push 시)
  deploy-production:
    needs: [test, security]
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v3
      - run: docker build -t ksnu-portfolio:${{ github.ref_name }} .
      - run: docker push ksnu-portfolio:${{ github.ref_name }}
      - run: kubectl set image deployment/api api=ksnu-portfolio:${{ github.ref_name }} -n production
      - run: kubectl rollout status deployment/api -n production
```

### 5.2 브랜치별 CI/CD 동작

```
┌─────────────────────────────────────────────────────────────┐
│  feature/tensorflow 브랜치                                   │
│  ├── Push → CI만 실행 (테스트 + 린트)                       │
│  └── PR 생성 → develop으로                                  │
│      ├── CI 실행 (테스트 + 린트 + 보안 스캔)               │
│      └── 배포 없음 (Merge 전까지)                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  develop 브랜치                                              │
│  ├── PR Merge 완료 시                                        │
│  │   ├── CI 실행 (전체 테스트)                             │
│  │   └── CD: Staging 서버 배포                             │
│  │       └── URL: https://staging.ksnu-portfolio.com       │
│  └── 개발자들이 기능 통합 및 테스트                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  release/v1.1.0 브랜치                                       │
│  ├── develop에서 생성                                        │
│  ├── QA 팀 테스트 (수동)                                    │
│  ├── 버그 수정 (hotfix 커밋)                                │
│  └── 준비 완료 시 main으로 PR                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  main 브랜치                                                 │
│  ├── release 브랜치 Merge 시                                 │
│  │   ├── CI 실행 (최종 검증)                               │
│  │   ├── CD: Production 배포                               │
│  │   │   ├── Vercel: Frontend                              │
│  │   │   ├── K8s: Backend                                  │
│  │   │   └── Rolling Update (무중단 배포)                  │
│  │   └── Git Tag 생성: v1.1.0                              │
│  └── 프로덕션 릴리스만 허용 (보호 브랜치)                   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  hotfix/security-patch 브랜치                                │
│  ├── main에서 긴급 생성                                      │
│  ├── 버그 수정 (1~2 커밋)                                   │
│  ├── PR → main (긴급 승인)                                  │
│  │   ├── CI/CD 즉시 실행                                   │
│  │   └── Production 즉시 배포                              │
│  └── main → develop으로 백포트 (동기화)                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. Git 명령어 워크플로우

### 6.1 일상적인 개발 워크플로우

```bash
# ========== 1. 새 기능 개발 시작 ==========

# develop 최신 상태로 업데이트
git checkout develop
git pull origin develop

# 새 feature 브랜치 생성
git checkout -b feature/comment-system

# 작업 시작...


# ========== 2. 작업 중간 저장 (Stash) ==========

# 현재 작업 임시 저장 (긴급 버그 수정 필요 시)
git stash save "WIP: comment validation logic"

# 다른 브랜치로 이동하여 긴급 작업
git checkout hotfix/urgent-bug
# ... 긴급 수정 완료 ...

# 원래 브랜치로 돌아와서 작업 복원
git checkout feature/comment-system
git stash pop


# ========== 3. 커밋 작성 ==========

# 변경 파일 확인
git status

# 특정 파일만 스테이징
git add src/components/Comments.jsx
git add src/services/api.js

# 또는 모든 변경 파일 스테이징
git add .

# 커밋 (Conventional Commits 규칙 준수)
git commit -m "feat(comments): Add comment submission form

- Implement form validation (max 500 chars)
- Add character counter UI
- Integrate with POST /api/comments endpoint
- Show success/error toast notifications

Refs #45"


# ========== 4. 원격 저장소에 Push ==========

# 최초 push (upstream 설정)
git push -u origin feature/comment-system

# 이후 push는 간단히
git push


# ========== 5. develop 브랜치 변경사항 가져오기 (Rebase) ==========

# 다른 개발자가 develop에 merge한 경우, 최신 상태로 동기화
git fetch origin develop

# 현재 브랜치를 develop 위에 재배치 (선형 히스토리 유지)
git rebase origin/develop

# 충돌 발생 시
git status  # 충돌 파일 확인
# 수동으로 충돌 해결 (파일 편집)
git add <충돌-파일>
git rebase --continue

# Rebase 취소하고 싶다면
git rebase --abort


# ========== 6. 커밋 히스토리 정리 (Interactive Rebase) ==========

# 최근 3개 커밋을 하나로 합치기 (PR 전 깔끔하게 정리)
git rebase -i HEAD~3

# 에디터에서:
# pick abc123 feat: Add comment form
# squash def456 fix: Validation bug
# squash ghi789 style: Format code
# → 3개 커밋이 1개로 합쳐짐


# ========== 7. Pull Request 생성 (GitHub CLI 사용) ==========

# GitHub CLI로 PR 생성 (웹 브라우저 없이)
gh pr create \
  --base develop \
  --head feature/comment-system \
  --title "feat: Implement comment system" \
  --body "## 변경 사항
- 댓글 작성 폼 구현
- 입력 검증 (500자 제한)
- API 통합

Closes #45"


# ========== 8. PR 리뷰 반영 후 수정 ==========

# 리뷰 피드백 반영
git add src/components/Comments.jsx
git commit -m "fix: Add error handling per review feedback"

# PR에 자동으로 추가됨
git push


# ========== 9. Merge 후 로컬 정리 ==========

# PR이 merge되면 로컬 브랜치 삭제
git checkout develop
git pull origin develop  # merge된 내용 가져오기

# 로컬 feature 브랜치 삭제
git branch -d feature/comment-system

# 원격 feature 브랜치 삭제 (GitHub에서 이미 삭제되었다면 생략)
git push origin --delete feature/comment-system

# 삭제된 원격 브랜치 정보 정리
git remote prune origin


# ========== 10. Release 준비 (develop → main) ==========

# Release 브랜치 생성
git checkout develop
git pull origin develop
git checkout -b release/v1.1.0

# 버전 번호 업데이트
echo "1.1.0" > VERSION
git add VERSION
git commit -m "chore: Bump version to 1.1.0"

# Release 브랜치 push
git push -u origin release/v1.1.0

# QA 완료 후 main으로 PR 생성
gh pr create --base main --head release/v1.1.0 --title "Release v1.1.0"

# Merge 후 Git Tag 생성
git checkout main
git pull origin main
git tag -a v1.1.0 -m "Release v1.1.0

Features:
- TensorFlow.js interest tracking
- Real-time monitoring UI

Bug Fixes:
- Comment deletion issue
- Scroll tracking accuracy"

# Tag push (자동으로 프로덕션 배포 트리거)
git push origin v1.1.0


# ========== 11. Hotfix (긴급 버그 수정) ==========

# main에서 hotfix 브랜치 생성
git checkout main
git pull origin main
git checkout -b hotfix/security-patch

# 버그 수정
git add backend/src/routes/comments.js
git commit -m "fix(security): Prevent XSS in comment content

- Sanitize user input with DOMPurify
- Add Content-Security-Policy header
- Update tests

Security: CVE-2024-12345"

# Hotfix push 및 PR 생성 (main으로)
git push -u origin hotfix/security-patch
gh pr create --base main --head hotfix/security-patch --title "Hotfix: Security patch"

# Merge 후 main → develop 백포트 (동기화)
git checkout develop
git merge main
git push origin develop


# ========== 12. Git 히스토리 조회 ==========

# 커밋 히스토리 보기 (그래프 형태)
git log --oneline --graph --all --decorate

# 특정 파일의 변경 이력
git log --follow -- src/hooks/useTensorFlow.js

# 특정 작성자의 커밋만 보기
git log --author="kgyujin"

# 특정 기간의 커밋만 보기
git log --since="2024-11-01" --until="2024-12-05"

# 커밋 메시지에서 키워드 검색
git log --grep="tensorflow"


# ========== 13. Git Blame (코드 작성자 확인) ==========

# 특정 파일의 각 라인을 누가 언제 작성했는지 확인
git blame src/hooks/useTensorFlow.js

# 특정 라인 범위만 보기
git blame -L 50,100 src/hooks/useTensorFlow.js


# ========== 14. Git Bisect (버그 발생 시점 찾기) ==========

# 이진 탐색으로 버그가 처음 발생한 커밋 찾기
git bisect start
git bisect bad  # 현재 커밋은 버그 있음
git bisect good v1.0.0  # v1.0.0은 정상 동작했음

# Git이 중간 커밋으로 이동, 테스트 후
git bisect good  # 또는 bad
# 반복...

# 버그 커밋 찾으면
git bisect reset  # 원래 브랜치로 복귀
```

### 6.2 고급 Git 기법

```bash
# ========== Cherry-pick (특정 커밋만 가져오기) ==========

# develop의 특정 커밋을 hotfix 브랜치로 가져오기
git checkout hotfix/security-patch
git cherry-pick abc123def456  # 커밋 해시
# 충돌 시 해결 후
git cherry-pick --continue


# ========== Revert (커밋 취소) ==========

# 특정 커밋을 되돌리는 새 커밋 생성 (히스토리 유지)
git revert abc123def456

# Merge 커밋 되돌리기
git revert -m 1 abc123  # -m 1: 첫 번째 부모 유지


# ========== Reset (커밋 완전 제거) ==========

# 주의: 원격 저장소에 push한 커밋은 reset하지 말 것!

# 최근 커밋 취소 (파일 변경사항은 유지)
git reset --soft HEAD~1

# 최근 커밋 취소 + 스테이징 해제 (파일 변경사항은 유지)
git reset HEAD~1

# 최근 커밋 취소 + 파일 변경사항도 완전 삭제
git reset --hard HEAD~1


# ========== Reflog (Git 시간 여행) ==========

# 모든 Git 작업 이력 보기 (reset으로 지운 것도 복구 가능)
git reflog

# 실수로 reset한 커밋 복구
git reset --hard HEAD@{5}  # reflog에서 찾은 위치로


# ========== Submodule (외부 저장소 포함) ==========

# 서브모듈 추가 (예: 공통 라이브러리)
git submodule add https://github.com/org/common-lib.git lib/common

# 서브모듈 초기화 (클론 후)
git submodule update --init --recursive

# 서브모듈 업데이트
git submodule update --remote


# ========== Worktree (여러 브랜치 동시 작업) ==========

# 새 worktree 생성 (별도 디렉토리에서 다른 브랜치 작업)
git worktree add ../ksnu-hotfix hotfix/urgent-bug

# 작업 완료 후 worktree 제거
git worktree remove ../ksnu-hotfix
```

---

## 📊 Git 통계 및 분석

### Commit 통계

```bash
# 전체 커밋 수
git rev-list --count HEAD
# 결과: 157 commits

# 작성자별 커밋 수
git shortlog -sn
# 결과:
#   142  kgyujin
#    10  GitHub Actions
#     5  Dependabot

# 파일별 변경 빈도
git log --pretty=format: --name-only | sort | uniq -c | sort -rg | head -10
# 가장 많이 수정된 파일 Top 10

# 코드 추가/삭제 통계
git log --shortstat --since="2024-11-01" | \
  awk '/files changed/ {files+=$1; inserted+=$4; deleted+=$6} \
  END {print "Files changed:", files, "\nLines inserted:", inserted, "\nLines deleted:", deleted}'
```

---

## 🎯 핵심 요약

### Git/GitHub 워크플로우 강점

1. **체계적인 브랜치 전략**
   - Git Flow 모델 적용 (main, develop, feature, hotfix, release)
   - 각 브랜치의 명확한 역할과 보호 규칙
   - Squash merge로 깔끔한 히스토리 유지

2. **자동화된 CI/CD 통합**
   - 브랜치별 다른 CI/CD 동작 (feature → CI만, develop → Staging 배포, main → Production 배포)
   - PR 생성 시 자동 테스트 + 보안 스캔
   - Git Tag 기반 Semantic Versioning (v1.1.0)

3. **코드 품질 관리**
   - Conventional Commits 규칙 준수
   - 2명 이상 코드 리뷰 필수 (main 브랜치)
   - PR 템플릿으로 표준화된 문서화

4. **효율적인 협업**
   - GitHub CLI로 브라우저 없이 PR 관리
   - Interactive Rebase로 커밋 히스토리 정리
   - Stash, Cherry-pick, Bisect 등 고급 Git 기법 활용

---

**발표 강조 포인트:**
> "이 프로젝트는 Git Flow 브랜치 전략을 엄격히 적용하여 feature, develop, release, main 브랜치를 분리 운영합니다. 157개 이상의 커밋을 Conventional Commits 규칙에 따라 작성했으며, 모든 PR은 2명 이상의 리뷰어 승인과 자동화된 CI/CD 파이프라인 통과를 거쳐야만 merge됩니다. Git Tag 기반 Semantic Versioning으로 버전을 관리하며, 각 릴리스는 자동으로 프로덕션에 배포됩니다."
