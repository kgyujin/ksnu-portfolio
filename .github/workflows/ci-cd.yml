name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  DOCKER_IMAGE_NAME: portfolio-web
  DOCKER_REGISTRY: ghcr.io

jobs:
  # CI 작업: 빌드 및 테스트
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout 코드
      uses: actions/checkout@v4

    - name: Docker 빌드 가능 여부 확인
      run: |
        echo "🔍 Docker 이미지 빌드를 시작합니다..."
        docker --version

    - name: Docker 이미지 빌드
      run: |
        docker build -t ${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }} .
        docker tag ${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }} ${{ env.DOCKER_IMAGE_NAME }}:latest

    - name: 환경변수 설정 (CI용)
      run: |
        echo "🔧 CI 환경을 위한 환경변수 설정..."
        echo "MONGODB_URI=mongodb://localhost:27017/test" >> $GITHUB_ENV
        echo "DB_NAME=test" >> $GITHUB_ENV
        echo "NODE_ENV=test" >> $GITHUB_ENV

    - name: Docker Compose로 서비스 테스트
      run: |
        echo "🧪 Docker Compose를 사용하여 서비스를 테스트합니다..."
        docker compose up -d
        sleep 10
        # 웹 서비스가 정상적으로 응답하는지 확인
        curl -f http://localhost:8080 || exit 1
        echo "✅ 웹 서비스가 정상적으로 실행되고 있습니다!"

    - name: 서비스 로그 확인
      if: always()
      run: |
        echo "📋 웹 서비스 로그:"
        docker compose logs web
        echo "📋 API 서비스 로그:"
        docker compose logs api || echo "API 서비스 로그 없음"

    - name: Docker Compose 정리
      if: always()
      run: |
        docker compose down -v

    - name: 빌드 결과 요약
      run: |
        echo "✅ CI 단계 완료!"
        echo "- Docker 이미지가 성공적으로 빌드되었습니다."
        echo "- 서비스가 정상적으로 시작되고 응답합니다."

  # CD 작업: Docker 이미지 푸시 (main 브랜치에 머지될 때만 실행)
  push-to-registry:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    permissions:
      contents: read
      packages: write
    
    steps:
    - name: Checkout 코드
      uses: actions/checkout@v4

    - name: GitHub Container Registry 로그인
      uses: docker/login-action@v3
      with:
        registry: ${{ env.DOCKER_REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Docker 메타데이터 추출
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.DOCKER_REGISTRY }}/${{ github.repository_owner }}/${{ env.DOCKER_IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Docker 이미지 빌드 및 푸시
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

    - name: 배포 완료 알림
      run: |
        echo "🚀 Docker 이미지가 성공적으로 푸시되었습니다!"
        echo "이미지 태그: ${{ steps.meta.outputs.tags }}"

  # 선택사항: Docker Hub에 푸시 (주석 해제 후 사용)
  # push-to-dockerhub:
  #   runs-on: ubuntu-latest
  #   needs: build-and-test
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   
  #   steps:
  #   - name: Checkout 코드
  #     uses: actions/checkout@v4
  #   
  #   - name: Docker Hub 로그인
  #     uses: docker/login-action@v3
  #     with:
  #       username: ${{ secrets.DOCKERHUB_USERNAME }}
  #       password: ${{ secrets.DOCKERHUB_TOKEN }}
  #   
  #   - name: Docker 이미지 빌드 및 푸시
  #     uses: docker/build-push-action@v5
  #     with:
  #       context: .
  #       push: true
  #       tags: |
  #         ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:latest
  #         ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }}
