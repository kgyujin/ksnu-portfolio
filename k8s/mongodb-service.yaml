# MongoDB 서비스 (클러스터 내부에서만 접근 가능)
apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: portfolio
  labels:
    app: mongodb
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb
