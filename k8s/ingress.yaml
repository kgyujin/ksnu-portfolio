# Ingress - 외부에서 API 접근을 위한 라우팅
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portfolio-ingress
  namespace: portfolio
  annotations:
    # Nginx Ingress Controller 사용 시
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # CORS 설정
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "Content-Type, Authorization"
    # Rate Limiting
    nginx.ingress.kubernetes.io/limit-rps: "100"
spec:
  rules:
  - host: api.portfolio.example.com  # 실제 도메인으로 변경 필요
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: portfolio-api-service
            port:
              number: 80
  # TLS 설정 (Let's Encrypt 사용 시)
  # tls:
  # - hosts:
  #   - api.portfolio.example.com
  #   secretName: portfolio-tls-secret
